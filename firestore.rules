rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /devices/{deviceId} {
      allow read, write: if true; // Explicitly allow device registration/checking
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
